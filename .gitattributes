# Consistent line endings across platforms: store + check out text as LF.
* text=auto eol=lf

# Treat these as binary (never normalize / diff).
*.png   binary
*.ico   binary
*.icns  binary
*.ttf   binary
*.woff  binary
*.woff2 binary
