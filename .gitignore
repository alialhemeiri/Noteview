# ── Dependencies ─────────────────────────────────────────────
node_modules/
app/node_modules/

# ── Frontend build output ────────────────────────────────────
app/dist/
app/dist-ssr/
*.local

# ── Rust / Tauri build output (large) ────────────────────────
app/src-tauri/target/
app/src-tauri/gen/schemas/

# ── Generated installers / portable launcher (build artifacts) ─
# These are copies of the release build. Delete these lines if you
# deliberately want the built binaries committed to the repo.
/Noteview.exe
/Noteview-Setup.exe
*.msi

# ── Logs ─────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── OS / editor cruft ────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Agent / tool local files (Claude Code, Codex, etc.) ──────
skills-lock.json
.claude/
.codex/
.agents/

# ── Internal docs — kept local, NOT published ────────────────
handoffs/
audits/

# ── Smoke-test scratch ───────────────────────────────────────
app/scripts/.smoke-tmp/
